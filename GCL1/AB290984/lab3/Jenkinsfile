pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'git clone https://github.com/InzynieriaOprogramowaniaAGH/MDO2023.git'
                sh 'docker build -f MDO2023/GCL1/AB290984/lab2/Dockerfile -t test123 .'
            }
        }
    }
}
